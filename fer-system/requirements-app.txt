# Extra deps for the Gradio demo (training env stays lean).
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-app.txt
#
# OpenCV 5 dropped cv2.CascadeClassifier; pin 4.x so Haar ships in-package
# with no extra model download.
gradio>=4.0.0
opencv-python-headless>=4.8.0,<5
